# Animated Developer Portfolio Hub

A static React + Vite portfolio hub with Framer Motion animations, responsive project cards, and category filtering.

## Scripts

- `npm run dev` - start local development server
- `npm run lint` - lint source files
- `npm run build` - generate static production assets in `dist/`
